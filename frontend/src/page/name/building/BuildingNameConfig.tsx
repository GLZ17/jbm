import ConfigRoute from "../../../route/ConfigRoute";
import ConfigName from "../common/ConfigName";

const BuildingNameConfig = ConfigName(
    'jbm_name_building',
    ConfigRoute.building_name.name
);

export default BuildingNameConfig;
