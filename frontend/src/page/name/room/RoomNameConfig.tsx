import ConfigRoute from "../../../route/ConfigRoute";
import ConfigName from "../common/ConfigName";

const RoomNameConfig = ConfigName(
    'jbm_name_room',
    ConfigRoute.room_name.name
);

export default RoomNameConfig;

